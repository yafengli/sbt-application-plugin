#!/bin/bash

if [ -z $JAVA_OPTS ];then 
    echo "JAVA_OPTS not set. eg.[JAVA_OPTS=-server -Dfile.encoding=utf-8]"
fi

LIB_PATH=lib
#common variable
PWD=$(cd `dirname $0`; cd ..; pwd)

#APP_CP=.
#for f in $(find ${PWD}/lib -name *.jar);do
#   APP_CP=$APP_CP:$f
#done

APP_CP=.:{{libStr}}
CLASSPATH=$APP_CP
export CLASSPATH

$JAVA_HOME/bin/java $JAVA_OPTS -cp $APP_CP {{mainClass}} $*
